version: "3"
services:
  # servicea:
  #   build: ./service_a
  #   depends_on:
  #     - mysql
  #   ports:
  #     - 8080:8080
  # serviceb:
  #   build: ./service_b
  #   ports:
  #     - 8081:8081
  mysql:
    image: mysql:5.7
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: users
      MYSQL_USER: users
      MYSQL_PASSWORD: users